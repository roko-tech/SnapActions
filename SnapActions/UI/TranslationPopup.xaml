<Window x:Class="SnapActions.UI.TranslationPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Translate" WindowStyle="None" ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource BackgroundBrush}"
        ShowInTaskbar="False" ShowActivated="False" Topmost="True"
        Width="520" Height="620" MinWidth="360" MinHeight="320">
    <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <DockPanel Margin="0,0,0,8" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Button DockPanel.Dock="Right" Content="X" Width="28" Height="28"
                        AutomationProperties.Name="Close translation" Click="Close_Click"
                        Background="Transparent" Foreground="{DynamicResource TextSecondaryBrush}"
                        BorderThickness="0" Cursor="Hand"/>
                <StackPanel>
                    <TextBlock Text="Translate" FontSize="16" FontWeight="SemiBold"
                               Foreground="{DynamicResource AccentBrush}"/>
                    <TextBlock Text="Google Translate" FontSize="11"
                               Foreground="{DynamicResource TextSecondaryBrush}"/>
                </StackPanel>
            </DockPanel>
            <StackPanel x:Name="StatusPanel" Grid.Row="1" Margin="0,0,0,10">
                <TextBlock x:Name="StatusText" Text="Opening Google Translate…" TextWrapping="Wrap"
                           Foreground="{DynamicResource TextSecondaryBrush}" FontSize="13"/>
                <Button x:Name="RetryButton" Content="Retry" Visibility="Collapsed" HorizontalAlignment="Left"
                        Margin="0,8,0,0" Padding="12,5" Click="Retry_Click" Cursor="Hand"
                        Background="{DynamicResource SurfaceBrush}" Foreground="{DynamicResource TextBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"/>
            </StackPanel>
            <Grid x:Name="BrowserHost" Grid.Row="2" Background="White"/>
        </Grid>
    </Border>
</Window>
